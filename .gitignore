# --------------------------------------------------------------------------
# 1. PYTHON / DJANGO BACKEND FILES (UNSAFE & UNNECESSARY)
# --------------------------------------------------------------------------

# Ignore the virtual environment folder entirely
venv/
.venv/
env/

# Django / Python temporary files
__pycache__/
*.pyc
*.log
*.sqlite3          # Ignore SQLite database files (even if you use Postgres)
*~
*.bak

# Django specific files
/backend/config/settings/local_settings.py  # A common pattern for private settings
/backend/config/settings/production.py      # Protect production settings
/backend/db.sqlite3                         # Database file

# Media and static file storage (often managed by the production server)
/backend/media/
/backend/static_root/

# Python environment artifacts
.env
.idea/
.vscode/

# --------------------------------------------------------------------------
# 2. REACT / JAVASCRIPT FRONTEND FILES (UNSAFE & UNNECESSARY)
# --------------------------------------------------------------------------

# Dependencies and Builds
/frontend/node_modules       # Ignore Node.js dependencies (installed via npm/yarn)
/frontend/dist               # Ignore the final build output directory (Vite/React build)
/frontend/build              # Ignore Create React App build output
/frontend/out                # Next.js export output (if used)

# Environment and Configuration Files (CRUCIAL!)
/frontend/.env               # Ignore the main .env file
/frontend/.env.local         # Ignore local environment variables (where your API key/URLs are)
/frontend/.env.*.local       # Ignore all local environment files

# Logs and Lock Files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json            # Unless you want to ensure deterministic installs
.history

# --------------------------------------------------------------------------
# 3. GLOBAL / SENSITIVE KEYS (CRUCIAL FOR SECURITY)
# --------------------------------------------------------------------------

# SENSITIVE KEYS/SECRETS
secret_key.txt
*.key
*.pem
*.pfx
*.jks